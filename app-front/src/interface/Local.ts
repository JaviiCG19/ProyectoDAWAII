export interface Local {
  id: number;
  nombre: string;
  // agrega más campos cuando veas el JSON real
  // direccion?: string;
  // telefono?: string;
  // ruc?: string;
  // MEJOR DICHO AQUI NOSE QUE DEBERIA DE IR
}